#include<stdio.h>
int main()
{
	int i,n,a[100];
	printf("enter size\n");
	scanf("%d",&n);
	printf("enter elemnts\n");
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	for(i=0;i<n;i++){
		if(a[i]%2==0)
		printf(" even is %d\n",a[i]);
	}
	for(i=1;i<n;i++){
		if(a[i]%1==0)
		printf("odd is %d\n",a[i]);
	}
	

}
